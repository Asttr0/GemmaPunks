rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Cloud Storage is disabled for the Spark-plan hackathon build.
      // FastAPI processes evidence temporarily and discards the binary.
      allow read, write: if false;
    }
  }
}
